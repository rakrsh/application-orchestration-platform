# OpenFGA (Quickstart Notes)

This repository includes a reference `docker-compose.yml` which starts an `openfga` container on port `8081`.

Notes:
- Persist OpenFGA storage for real deployments.
- Place OpenFGA model files and tuple migration scripts under `auth/openfga/models` when ready.
